> このファイルは司令塔(assistant ワークスペース)の観測メモ。拘束力はない。
> コードや CLAUDE.md と矛盾する場合はそちらが正。参考にしてもしなくてもよい。

## このプロジェクトは何をしたい奴か

コミチ系マンガプラットフォーム（`comicpash.jp` や `comicride.jp` など）における無料チケット獲得ミッションおよび無料ガチャの消化を全自動化する個人用 Chrome 拡張機能（Manifest V3）。

### 目的・概要
ユーザーが各マンガサイトのミッションページ（`/mission`）を開き、拡張機能のポップアップから「一括自動実行を開始」を1クリックするだけで、未達成ミッション（作品の読了、お気に入り登録、ガチャ実行など）を自動解析・キュー化し、人間の操作をシミュレートして完了させるツール。

### 技術構成・スタック
- **仕様/形式**: Chrome Extension Manifest V3（ビルド工程なしのプレーン JavaScript 構成）
- **構成要素**:
  - `manifest.json`: Manifest V3 定義（Service Worker、Popup、Debugger 権限など）
  - `background.js`: Service Worker。全体のタスクキュー管理、アクティブタブの直列遷移制御、CDP（Chrome DevTools Protocol）による低レイヤー操作フォールバック
  - `content_mission.js`: ミッション一覧ページの DOM を解析して未完了ミッションを抽出するパーサー
  - `content_action.js`: 各作品ビューアーやガチャ画面に注入される操作スクリプト（横読み/縦読みのページ送り、お気に入り着脱、ガチャ操作）
  - `popup.html` / `popup.js`: 開始/停止およびリアルタイムログを表示するダークモード基調の UI

### 主要機能と設計上の特徴
1. **アクティブタブ1本での直列実行**:
   - バックグラウンドタブでのタイマー/rAF スロットリングによる処理遅延・停止を回避するため、アクティブタブ1本で1タスクずつ直列実行。
2. **多段階フォールバック機能付きページ送りドライバー**:
   - 縦読み・横読みビューアーの両方に対応。
   - 横読みでは高速シークバー操作（一括スライド）を試み、失敗時は段階的シークバー送り → 合成キーイベント → CDP (`chrome.debugger`) 経由のキー入力へと順次フォールバックして確実に「読了」状態を作る。
3. **お気に入りミッション自動解除**:
   - お気に入り登録ミッションにおいて、未登録作品の場合は登録してミッション達成後に即座に自動解除することで、ユーザーの本棚を汚さずに報酬を獲得できる。
4. **ガチャおよび X (Twitter) シェア連動対応**:
   - ミッション完了後の無料ガチャ実行および X シェア画面の自動処理による「もう1回まわす」ガチャまで全自動で完了する。
5. **堅牢性とループ防止**:
   - DOM 構造変更に強い複数候補セレクタ方式を採用。
   - URL 履歴チェックによる同話重複ループ防止機構を搭載。
